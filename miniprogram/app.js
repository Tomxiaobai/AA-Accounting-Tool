App({
  globalData: {
    baseUrl: 'http://127.0.0.1:3000',
    userId: 'user_default',
    userName: 'default_user'
  },

  onLaunch() {
    // 可以在这里获取用户信息或做初始化
    console.log('AA记账小程序启动');
  }
});
